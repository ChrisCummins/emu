set -ex
test_setup

source=source

# Create a sink
emu init
mkdir $source
emu sink add origin $source

# Make a snapshot and ensure the last line contains 'pass'
emu snapshot -V | tail -n2 | grep 'pass'

test_teardown
