set -ex
test_setup

source=source

# Create a sink
emu init
mkdir $source
emu sink add origin $source

# Push a snapshot and ensure the last line contains 'pass'
emu push -V | tail -n2 | grep 'pass'

test_teardown
