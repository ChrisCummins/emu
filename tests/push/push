set -e
test_setup

cd $EMU_TEST_DIR/test-source

assert_fail "emu push"
emu init -v
echo "hello, world!" > test
emu stack -v add origin ../test-stack
emu push
assert_strings_match "hello, world!" "$(cat ../test-stack/.emu/trees/*/test)"

test_teardown
