echo "TODO: implement as unit test"
# set -eux
# test_setup

# sink=../sink

# emu init
# mkdir -pv $sink
# emu sink add origin $sink

# emu push
# emu push
# emu push

# snapshot_id=$(ls $sink/.emu/nodes | head -n1)
# snapshot_name=$(ls $sink/ | head -n1)

# echo "snapshot id = $snapshot_id"
# echo "snapshot name = $snapshot_name"

# rm -rfv $sink/$snapshot_name
# assert_is_not_dir $sink/$snapshot_name

# set +e
# emu clean --dry-run --recursive
# set -e
# assert_is_dir $sink/$snapshot_name

# set +e
# emu clean --recursive
# set -e
# assert_is_not_dir $sink/$snapshot_name

# test_teardown
